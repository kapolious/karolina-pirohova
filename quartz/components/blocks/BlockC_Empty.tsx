import { BlockTemplate } from "./types"

/**
 * Block C placeholder. Currently always empty — the column space is reserved
 * but renders nothing. When you decide what goes here (a sketch, a quote, a
 * second listing per folder, etc.) make a new BlockC_Whatever.tsx template
 * and route it from BlockFrame.tsx.
 */
const BlockC_Empty: BlockTemplate = () => null

export default BlockC_Empty
